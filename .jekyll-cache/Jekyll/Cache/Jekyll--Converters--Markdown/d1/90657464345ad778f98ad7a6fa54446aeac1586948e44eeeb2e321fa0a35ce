I"i<p>In Open Peer Power 0.52 we introduced the first version of our script editor. If you just created a new configuration with Open Peer Power then you’re all set! Go to the UI and enjoy.</p>
<div class='videoWrapper'>
<iframe src="https://www.youtube.com/embed/_Rntpcj1CGA" frameborder="0" allowfullscreen></iframe>
</div>
<h2>Updating your configuration to use the editor</h2>
<p>The script editor reads and writes to the file <code>scripts.yaml</code> in your <a href="/docs/configuration/">configuration</a> folder. Make sure that you have set up the script integration to read from it:</p>
<figure class="highlight"><pre><code class="language-yaml" data-lang="yaml"># Configuration.yaml example
script: !include scripts.yaml</code></pre></figure>
<p>The content that was under <code>script:</code> should now be moved to <code>scripts.yaml</code> to be editable.</p>
:ET